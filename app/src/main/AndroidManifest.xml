<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.my_library">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.My_Library"
        tools:targetApi="31">
        <activity
            android:name=".activities.MyListActivity"
            android:exported="false"
            android:label="My Book Shelf"/>
        <activity
            android:name=".activities.AllBooksDetailActivity"
            android:exported="false"
            android:label="Books Detail"/>
        <activity
            android:name=".AboutActivity"
            android:exported="false" />
        <activity
            android:name=".FinishedReading"
            android:exported="false"
            android:label="Book Finished"/>
        <activity
            android:name=".CurrentlyReadingActivity"
            android:exported="false"
            android:label="In Progress"/>
        <activity
            android:name=".AllBooksActivity"
            android:exported="false"
            android:label="Books"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>